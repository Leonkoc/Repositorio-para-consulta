// Criar um algoritmo que verifica se uma palavra é um palindromo
// Palin é uma palavra ou frase que é igual de trás pra frente, ex: OVO, ANA,ARARA ,UUU
// const palin = require("prompt-sync")({ sigint: true });
// let entrada;
// entrada = palin("Digite um possivél palindromo: ");

// function verificarPalindromo(string) {
//   if (!string) return;

//   return string.split("").reverse().join("") === string;
// }
// console.log(verificarPalindromo("ovo"));
// //Var e let podem ser reatribuídas, mas apenas var pode ser redeclarada;
const meuObjeto = {
 chave: valor;
}

console.log(meuObjeto.chave); 
console.log(meuObjeto[chave]);

